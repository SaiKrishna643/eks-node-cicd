apiVersion: v1
kind: Service
metadata:
  name: devpulse
  namespace: devpulse
  labels:
    app: devpulse
spec:
  type: LoadBalancer
  selector:
    app: devpulse
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
